# Agents

## Cursor Cloud specific instructions

This repository ("Taxi Driver App") is currently an empty scaffold with only a `README.md`. There is no source code, no dependency manifest, no build system, no test framework, and no runnable application yet.

### Available system tools
- **Node.js** v22+ with npm v10+
- **Python** 3.12+
- **Git** 2.43+

### Development environment
- No dependencies to install — update script is a no-op.
- Once code is added, update the update script to match the chosen package manager (e.g., `npm install`, `pip install -r requirements.txt`).

### Lint / Test / Build / Run
- No lint, test, build, or run commands exist yet. These should be added alongside the first application code.
